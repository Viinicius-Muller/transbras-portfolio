import { LoginContent } from "@/components/login/login-content"

export const metadata = {
  title: "Login - TransBras",
  description: "Login to access the TransBras admin dashboard.",
}

export default function LoginPage() {
  return <LoginContent />
}
